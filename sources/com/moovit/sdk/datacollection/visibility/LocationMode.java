package com.moovit.sdk.datacollection.visibility;

import p810sz.C19577c;
import p810sz.C19584i;

public enum LocationMode {
    LOCATION_MODE_OFF,
    LOCATION_MODE_HIGH_ACCURACY,
    LOCATION_MODE_SENSORS_ONLY,
    LOCATION_MODE_BATTERY_SAVING,
    LOCATION_MODE_KITKAT_NETWORK_ONLY,
    LOCATION_MODE_UNKNOWN;
    
    public static final C19584i<LocationMode> CODER = null;

    /* access modifiers changed from: public */
    static {
        LocationMode locationMode;
        LocationMode locationMode2;
        LocationMode locationMode3;
        LocationMode locationMode4;
        LocationMode locationMode5;
        LocationMode locationMode6;
        CODER = new C19577c(LocationMode.class, locationMode, locationMode2, locationMode3, locationMode4, locationMode5, locationMode6);
    }
}
